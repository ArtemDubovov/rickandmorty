import {
    GetAllCharacters,
    GetAllEpisodes,
    GetEpisode,
    GetCharacterByID,
    GetInfoPages,
    GetCharactersByIDs,
} from './api'

export {
    GetAllCharacters,
    GetAllEpisodes,
    GetEpisode,
    GetCharacterByID,
    GetInfoPages,
    GetCharactersByIDs,
}
